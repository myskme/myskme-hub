# 《是猴就上100层》AI 接续说明

更新时间：2026-08-12

## 当前定名

- 正式中文名：`是猴就上100层`。
- 正式域名保持 `https://monkey.myskme.com/`，不因改名迁移网址。
- `《猴先生，楼上请》` 已被用户明确否决，不得恢复。
- “猴先生”继续作为角色名与文案称呼；旧 `myskme_monkey_upstairs_*` 存档键继续保留，避免玩家纪录丢失。

## 20260812.6 大版本

- 版本标记：`20260812.6-monkey-100-party-pwa`。
- 新增 2–6 人同设备轮流挑战：2–4 轮、45 / 75 / 105 秒可选；同一轮所有参赛者使用相同确定性楼层种子和道具分布，统一空手开局。累计总分排序，最高高度、最高连跳依次破同分。
- 多人比赛写入 `myskme_monkey_party_v1`，在交机和成绩页都可回首页，刷新后继续当前选手。多人赛不写单人最高分、单人榜、档案章或开工装备。
- HUD 同时显示层数与米数；300 米对应 100 层，达到时触发“谈话人又上了一层”的反高潮。
- 台阶视觉改为轮廓、材质、颜色、运动和破坏反馈。除绕口令悬浮词牌与标点机制外，平台表面不显示说明文字。
- 新增正式 PWA：`manifest.webmanifest`、`sw.js`、`icons/`。图标母源是与游戏同源的 SVG 猴先生与金色鱼小姐，PNG 提供 180、192、512 和 maskable 512；支持浏览器安装、iOS 添加到主屏幕和离线冷启动。
- MYSKME 作品总目同步新名称、封面描述和 2–6 人玩法。

## 20260812.7 惊喜分享与安卓适配

- 版本标记：`20260812.7-monkey-surprise-share-cool`。
- “复制坠落报告”已改为 1080 × 1350 图片成绩海报：海报记录本局印记、成绩、称号与二维码，按权重抽取六种版式；浏览器支持 Web Share 时只分享图片文件，不附可随意改写的成绩文字。
- 二维码代码固定使用同站 `vendor/qrcode-generator.js`，保留 MIT 许可并加入 service worker 预缓存；禁止替换成在线二维码接口或外域脚本。
- 安卓宽虚拟视口兜底仅在触屏、竖屏、宽度至少 600px 且长宽比至少 1.45 时启用，避免影响普通桌面和横屏平板。复现入口为 `?wideqa=1`，980 × 2048 下游戏外框应约 960 × 1761。
- 鱼式救援改为可感知的上推动量：角色回到安全画面、向上弹起约 20 米，并在脚下生成三秒临时泡泡台；`?rescue=1` 的 `rescueLift` 应为 105。
- 新增十二种洗牌式小意外、五个固定高度暗号和长期“意外图鉴”。随机小意外在抽完一袋前不重复，多人赛和测试参数不写图鉴。
- 音频主总线加入 DynamicsCompressor 限制器，噪声与振荡器统一渐入渐出，降低轻微爆音；触屏主循环上限 50 fps、音乐调度改为 70ms，菜单/结算停止 rAF，隐藏页与空闲音频自动挂起。
- 海报验收入口为 `?posterqa=1`；发布门禁增加本站二维码脚本、六档版式、十二种意外、安卓宽视口、救援平台、空闲 rAF 与音频挂起检查。

## 20260812.8 可达路线修复

- 版本标记：`20260812.8-monkey-reachable-route`，Service Worker 缓存同步升为 `myskme-monkey-20260812-8`。
- 修复少数随机种子会生成“看得见却跳不到”的主路线。生成器现在以实际承重台为路线锚点，并按该台真实弹力反解下一层允许的最大上升距离；普通台、弹舌台等不同弹力不再套用同一个 132 像素上限。
- 路线判定只约束每排至少一条可达主路，旁支仍保留风险、绕行和意外感；不会把所有平台铺成等高台阶。
- 内置 `?qa=1` 静态检查新增逐排物理可达断言；`?stress=1` 继续覆盖 24000 帧运行压力与角色可见性。以后调整重力、跳力或平台类型时，必须同步更新可达上限函数并重跑两项门禁。

## 发布门禁

每次修改必须至少执行：

1. `node monkey/tools/extract-assets.mjs`
2. `node monkey/tools/extract-assets.mjs --check`
3. `node deploy/monkey/build-edgeone-monkey.mjs <新的 /tmp zip 路径>`
4. 浏览器打开 `?qa=1`、`?partyqa=1`、`?stress=1`
5. 390 × 844、320 × 568、1280 × 720 检查横向溢出、主流程和猴先生完整可见
6. 多人至少实测设置、交机、限时结算、同轮种子一致、排名和刷新续赛
7. 在线发布后检查首页、manifest、192 图标、vendor 二维码脚本、service worker、海报生成与离线冷启动

角色消失是零容忍回归：不得给 SVG `<use>` 添加水平镜像，不得删除死亡冻结、屏幕钳制、最近安全点修复或 24000 帧压力测试。

### 2026-08-12 · Claude · 鱼小姐按 canon 改回青绿（并立法防第四次）

- **为什么改**：鱼小姐是绿鲤鱼成精，canon 上不存在金鲤鱼。她已经被改成金色两次，
  第二次连交接文档和 PWA 图标描述都跟着写成「金色鱼小姐」——**错误被转正了**，
  这才是最危险的地方：下一个接手的人会照着描述继续画金鱼。
- **改了哪些**（只动填色，造型一律没碰）：
  - 游戏本体 `index.html` 里的 `art-fish` symbol 7 处填色（`fishScene` 用的就是它，所以游戏内立绘同步变了）
  - 图标 `icons/monkey-100.svg` 2 处填色，以及那句写着「金色鱼小姐」的 `<desc>`
  - 四张 PNG 图标重新出图；**maskable 那张保持原有的等比 80% 居中安全区构图**
  - `reusable-assets/` 重新生成；`fish-gold.svg` 改名 `fish.svg`——文件名带颜色，下次换色它就变成骗人的名字
  - 素材调色板 `fishGold` 改为 `fish`/`fishFin`；本作 UI 用的 `teal: #1f9e8e` 原样不动
- **色值怎么定的**：`#3fab84`（鳍 `#6cd0af`，泡泡 `#3fa27a`）。做法是**保留金色原本的明度与饱和结构**，
  只把色相转到本作已有的青绿家族。试过四组：直接沿用 UI 的 `#1f9e8e` 太暗、会和描边 `#2f5148` 糊在一起；
  纯色相旋转出来是荧光薄荷绿，跟这套克制手绘风打架。最终这版跟描边拉得开轮廓，也和水面 `#dff3ef` 呼应。
  旁证：游戏里本来就把鱼当青色对待——鱼主题皮肤 accent 是 `#1f9e8e`，鱼救援的 toast 样式直接叫 `'teal'`。
- **版本与缓存**：`20260812.9-teal-fish-canon`，SW 缓存升到 `-9`。
  这次是**用户可见的美术改动**，必须 bump，否则已装到主屏幕的人一直看到金鱼。
  （反过来说：只动看不见的 meta 标记时不要 bump，那会让所有人白白重下全部资源。）
- **立法防第四次**：构建期与线上验收两道门都改成**守青色**——
  既断言青绿在、也断言旧金色不在。谁再把她改回金色，构建/发布直接失败。
  这一条与版本号那种「要从源头现取」的东西不同：**canon 是约束，就该写死在门里**。
  已反向验证：把填色改回 `#e3ad32`，构建正确拦下并报「canon 无金鲤鱼」。

### 2026-08-12 · Claude · 修「成绩海报里还是金鱼」——SW 装了旧 HTTP 缓存

- **现象**：鱼小姐改青绿并发布后，游戏内立绘确实变青了，但**成绩海报里那条鱼还是金的**。
- **不是没改干净**：仓库、发布包都验过零金色（把 zip 解开取图标，泡泡区域全是 `#3fab84`/`#6cd0af`）。
- **真因**：海报会把 app 图标 `icons/monkey-100-512.png` 整个画进左上角（`loadPosterIcon`），
  而 `sw.js` 安装时用的是 `cache.addAll(CORE)`——**它走浏览器自己的 HTTP 缓存**。
  EdgeOne 给图片设了长缓存，于是新版 SW 把旧的金鱼 PNG 原封不动装进了新缓存。
  页面 HTML 是导航网络优先所以是新的，图标却是旧的，**版本号明明升了、资源却是旧的**。
- **修法**：安装改成逐个 `fetch(new Request(url,{cache:'reload'}))` 再 `cache.put`，绕开 HTTP 缓存。
  缓存版本升到 `-10`（已经装了 `-9` 但里面是脏资源的人，需要一次新的安装轮）。
- **自鸣棋早就修过同一个坑**（见其协作日志 0810「网络加固」那条：安装时用 `cache:'reload'`
  防止新 SW 搭配旧 HTTP 缓存）。同一个生态里踩两次，所以这次把它写成门禁：
  构建期断言 `sw.js` 里必须出现 `cache:'reload'`，拿掉就构建失败。已反向验证。
- **教训**：判断「改没改上线」不能只看页面，要看**那个资源自己是怎么被缓存的**。
  导航网络优先 ≠ 所有资源都新；恰恰是这种混合策略最容易造出「一半新一半旧」的现场。

### 2026-08-13 · Claude · 暂停与返回主菜单、称号系统、红鲤鱼帽

- 版本标记：`20260813.1-pause-titles-carp-hat`，SW 缓存同步升为 `myskme-monkey-20260813-1`。
  这次是用户可见的功能与美术改动，必须 bump。

**暂停（本次最容易做错的一件事）**

- 这个游戏所有的限时效果——磁卡、宽容尺、电梯欠条、金猴、审讫红边、房租台、泡泡台、
  多人赛倒计时——记的都是 `performance.now()` 上的**绝对时刻**。所以「停掉 rAF」根本不是暂停：
  时间照走，暂停十秒等于白送十秒道具时间和十秒比赛时间。**真暂停 = 恢复时把所有绝对时刻整体后移**。
- 三张登记表 `PAUSE_TIME_FIELDS` / `PAUSE_PLAYER_TIME_FIELDS` / `PAUSE_ENTITY_TIME_FIELDS`
  列出所有要后移的字段，`PAUSE_NON_TIME_FIELDS` 列出名字像但不在这条时间轴上的（米数阈值、`pausedAt`）。
  **以后新加任何 `xxxUntil` / `xxxAt` 字段，必须登记进去**，否则：
  - 构建期：`build-edgeone-monkey.mjs` 静态扫源码里所有以 Until / At 结尾的字面量键与登记表对账，漏一个就构建失败；
  - 运行期：`?qa=1` 的「暂停计时字段登记齐全」扫活对象，同样抓。
  两道门都反向验证过（删掉 `magnetUntil`、新增一个没登记的 `coffeeUntil`，都被当场拦下）。
  注意门里写死的是「一个都不许漏」这条约束，不是「一共几个字段」那种会过期的镜像。
- `?qa=1` 另有「暂停会把所有绝对计时整体后移」一条：真跑一次 pause/resume（可传假时钟），
  断言剩余时长分毫不差，做完把动过的字段还原。故意让 `shiftDeadlines` 漏移，它会 FAIL。
- 顺带补上一个旧漏：原来 `visibilitychange` 只停 rAF、不停计时，切后台十秒回来照样白送十秒。
  现在切后台走同一条 `pauseGame()`，回到前台停在暂停面板上，由玩家自己点继续。
- 返回主菜单要二次确认（同一个按钮点两次），避免手滑丢进度。多人赛里这个按钮改成
  「结束本轮并交接」，走正常 `finishRun('timeout')`，**不允许直接跑回首页**——否则这一轮悬着、后面的人没法接。

**称号**

- 十六个长期解锁称号，与结算页按高度评的五档职级（`TITLES`）不是一回事，别混。
  存 `myskme_monkey_career_v1` 的 `titles` / `worn` / `tally`；老存档缺这几段按 0 起算，不迁移、不清档。
- 铁规矩：`HONORS[].test` 只许读 `honorContext()` 给的字段，不许直接摸 `state` / `career`，
  否则自检没法喂假数据，这张表就变成没人验得了的东西。
- 质检局（`IS_TEST_RUN`）与多人局都不写称号与跨局计数。

**红鲤鱼帽与奶茶定制单**

- 新增第十三种随机意外：猴先生戴上红鲤鱼帽假扮红鲤鱼，驴探头说「仿的」，鱼小姐游过来「没有形成看法」。
  纯装饰，`?qa=1` 有一条断言它不碰任何数值。
- **红鲤鱼不是 canon 角色**，这顶帽子必须是红的（`#e0452c`）。做成青绿它就是鱼小姐本人，梗当场作废——
  构建与线上验收都有门挡着，与「鱼小姐不许改回金色」是同一类立法。
- 奶茶定制单只是结算页与海报上的一句玩笑（茶底看结束部门、甜度看香蕉、冰量看连跳），不参与任何数值。
  当初考虑过做成真机制（茶香版/微糖/去冰改数值），评估下来会把「点左点右往上」这个极简手感搅浑，放弃。

**踩到并写成门禁的两个坑**

- **状态类名撞了资源类名**：给帽子的 `<use>` 用了 `.carp-hat`，戴帽子时又给 `#gameShell` 加了同名的
  `.carp-hat`，于是外壳自己命中 `.carp-hat{display:none}`，**整个游戏画面当场消失**——而所有数值自检照样全绿，
  是靠截图才看见的。状态类改名 `carp-hat-on`，并加了「戴上帽子后外壳/舞台/角色都不能是 display:none」的自检
  与构建断言。教训：改了画面就要看画面，别只信断言。
- **`.btn.danger` 写在 `.btn.secondary` 前面**，同权重被后者盖掉，红调根本没生效。挪到
  `.btn.install` / `.btn.resume` 那一行旁边——那里本来就是按钮配色变体的集中地。
- 另外修了 `wrapCanvasText(...,maxLines=1)` 的一个老毛病：`used` 在 push 之前算，恒为 0，
  于是放得下的单行文本也会被补一个省略号。单行文本改走新的 `fitCanvasLine()`。

**美术资源复利（王老师 0813 提的长期要求）**

- 目标是让美术能反复用：出周边、做别的作品、喂给二创、给世界观图鉴当立绘。
  做法不是「另存一份」，而是**正本唯一、机器导出镜像**：
  `reusable-assets/` 里现在除了 SVG，还自动生成 README.md（清单 + 画风参数 + 调色板 + 二创须知）
  和 schema 2 的 `asset-manifest.json`（每张图的 symbol、画布尺寸、SHA-256、在世界观里是谁）。
  画风参数是**从正本里现数出来的**（描边色按出现频次取），不是手写在文档里的一份会过期的镜像。
- **导出器 `--check` 从「交接文档里的一条手工步骤」升格为发布门禁**：改了正本美术却没重跑导出器，
  构建直接失败。已反向验证。以前它只写在文档里，漏跑就悄悄过期。

**顺带**：把道具图标里的 `↑` 换成「梯」——箭头字符属于 0725 emoji 全局禁令的范围，这是历史遗留。

### 2026-08-13 · Claude · 手机全屏（大屏手机上下留边）与红鲤鱼帽重画

**「加到桌面启动不是全屏」的真因，不是 PWA 声明，是一个写死的高度**

- 声明其实一直是齐的：`apple-mobile-web-app-capable`、manifest `display: standalone`、
  `viewport-fit=cover`、`env(safe-area-inset-*)` 都在。问题出在 CSS：
  `#gameShell{width:min(100vw,460px);height:min(100svh,844px)}`。
  那个 **844 是 iPhone 13 / SE 时代的屏幕高度**，写死之后就成了一份会过期的镜像。
- 实测（Chromium 真跑，不是估算）：

  | 设备 | 视口 | 上下留边 |
  |---|---|---|
  | iPhone 15 | 393 × 852 | 8px |
  | iPhone 15 Pro Max | 430 × 932 | **88px** |
  | iPhone 16 Pro Max | 440 × 956 | **112px** |
  | Pixel 8 | 412 × 915 | **71px** |

  而且露出来的是 `#d9ccb1`（桌面「手机框」外的底色），和纸色 `#faf3e2` 不同，所以特别显眼。
- **修法**：`@media(max-width:699px)` 下铺满 `100vw × min(100svh, 100vw*2.5)`，去掉左右黑边与投影，
  底色改成纸色。`≥700px` 的桌面手机框造型原样保留。
- **顺带发现**：现代手机的长宽比几乎正好是本作的设计比 844/390 = 2.164——
  iPhone 15 Pro Max 是 932/430 = 2.167。**铺满之后反而比原来封顶时更不变形**
  （原来 Pro Max 上是 -9.3% 的纵向压缩，现在是 +0.2%）。那个 844 封顶是纯粹的损失。
  2.5:1 的收口只对超长安卓设备生效，那种情况下露出来的边也是纸色，看不出来。
- 三个安卓宽虚拟视口的兜底路径（`html.wide-mobile-viewport`）必须保持固定盒再整体缩放，
  所以给它加了一条更高优先级的规则把固定盒还回去，别被铺满规则带偏。已实测 980×2048 下外框仍是 960×1761。
- **iPhone 上还有一个不是代码的坑**：只有 **Safari** 的「添加到主屏幕」才产生独立全屏应用；
  Chrome / Edge / 微信内置浏览器加出来的只是书签，点开仍带地址栏，用户会以为是游戏没做全屏。
  安装说明页现在第一条就写这句，并且检测到 iOS 上的非 Safari 浏览器时会直接点破。
- 四条门禁已加并反向验证：铺满规则本身、PWA 全屏声明、安全区声明、安卓宽视口固定盒。
  改回 844 封顶、删掉任一条声明，构建都会失败。

**红鲤鱼帽重画**

- 第一版太像「一块红色」。重画后按真鲤鱼的识别特征来：**分叉尾鳍**、背鳍、鳃盖线、
  鳞纹弧线、**两根胡须**（鲤鱼最强的识别符号）、带浅色眼白圈的大眼睛、腮红。
- 排版上踩到两次：眼圈 / 鳃线 / 嘴挤在左端糊成一团（把眼睛上移、鳃线右移、嘴推到吻尖解决）；
  背鳍没有生长空间（鱼背压得太高，把整条鱼下移 4 并压扁一点才腾出鳍位）。
- 垂直预算是硬的：帽子底边不能低于 y≈35，否则会盖住猴先生的眼睛（眼睛在头部坐标 y≈43）。
  胡须可以垂到 y≈40，正好在眉毛边上晃，反而更好玩。
- 三个姿势共用同一个头部 symbol（`art-monkey-head-base` 在三姿里都是 `0,0 100×74`），
  所以一顶固定坐标的帽子在三姿里都对得上，不需要每姿势画一顶。

### 2026-08-13（下午）· Claude · 拆多人模式、奶茶图鉴、连跳上限修正

**拆掉 2–6 人多人模式**（王老师 0813 决定）

- 影响面比看上去大：121 处引用，约 1.9 万字符。拆的是三块屏、一套 localStorage 存档
  （`myskme_monkey_party_v1`，老玩家机器上还留着，不管它，读不到就是了）、
  一组常量与函数，以及散在 `startRun` / `finishRun` / `loop` / `resetWorld` / `updateMissionUI` /
  暂停面板 / 屏幕列表 / 事件绑定里的十几处分支。
- 做法：**逐处精确替换 + assert**，不用模糊正则。中途踩到一次：按「函数名到函数名」整段删时，
  `renderPartySetup → ensureGameLoop` 之间夹着 `loop()`，差点把主循环一起删掉。
  教训是**删整段之前先把区间两端和中间夹着谁看清楚**，别凭函数名的语义猜顺序。
- `state.mode` 一并去掉（曾经只有 'solo' / 'party' 两个值），`.party-running` 类、
  运行时报告里的 party 字段、`?partyqa=1` 自检模式与那条自检项都跟着删了。
- **门禁反转**：原来构建期断言「多人模式代码必须在」，现在断言 `!/\bparty\b/i` ——
  别再被顺手粘回来。要恢复是一次正式决定，不是一次粘贴。
- 对外口径三处跟着改：`monkey/manifest.webmanifest` 的 description、页面 `<meta name="description">`、
  主页 `build_hub.py` 的 tag 与 desc。**主页那处照规矩把上一版原文加进了 `LEGACY_DEFAULT_DESC`**，
  否则老访客（包括王老师自己那台）存档里存着旧介绍，永远刷不出新文案。

**奶茶图鉴（二十款）**

- 王老师原话是「奶茶风味可以换种形式，比如完成什么成就解锁各式各样的奶茶图鉴」。
  于是把上一版那句一次性的「本局奶茶定制单」升级成可收集的图鉴。
- **三套收集的分工是死规矩**：意外图鉴＝你遇到了什么（见闻），称号＝你是谁（身份），
  奶茶＝你喝到了什么（战利品）。奶茶的解锁一律挂在「本局做过的具体动作」上，比称号密、比称号好拿，
  负责小步反馈；称号负责慢与稀有。
- 为此加了 `state.seen`（一个 tag 集合），在落台、用道具、大事件、鱼式救援、固定暗号这几处打点。
  以后要加新的解锁条件，先在动作发生的地方 `state.seen.add(...)`，别去反推。
- 杯子是一个函数画的（`teaCupSvg`），二十款只换液体色与珍珠色——所以加新品只要加一行数据。
- **自检当场抓到一次真错**：`talker` 这个 id 同时出现在称号与奶茶里，两边都是「上到 1020 米」，
  等于同一件事发两份奖。那条「奶茶与称号的解锁条件不重叠」的门就是为这个写的，已改挂到固定暗号上。

**连跳上限：一个让称号永远解不开的写死值**

- 跑了 40 局自动驾驶模拟收分布，发现 `maxFlow` 的**最大值恰好是 9**——
  查代码：`state.flow=…?Math.min(9,state.flow+1):1`。而新加的称号「比猴先生还猴」要 12 连跳，
  **永远解不开**。
- 修法：提成具名常量 `FLOW_CAP=20`。加分本来就另有上限（`Math.min(4,Math.floor(flow/3))`），
  所以放开不会通货膨胀；音乐强度那条按原来的 9 封顶，免得编曲一路顶满。
- **方法上的一条**：这个错光读代码很难发现（两处相隔几百行，各自都合理）。是**跑模拟看分布**
  看出来的——「最大值恰好等于某个整数」就是有硬上限的信号。以后加带数值门槛的解锁，
  先拿模拟跑一遍分布，别拍脑袋定阈值。

### 2026-08-13（晚）· Claude · 蛇 / 蛋 / 驴 三位重做

王老师的要求：「形象一般，可以参考猴与鱼风格，形象可以统一并且借鉴类似于 jellycat 等情绪治疗」。

**为什么原来的三位比猴与鱼弱** —— 不是造型问题，是**结构层次**问题。
猴先生有浅色脸盘、鱼小姐有奶油肚皮，再加白高光和腮红；而蛇就是一根平的绿管子、
蛋是一个白水滴。把「深色主体 + 浅色肚皮/脸盘 + 大眼白高光 + 腮红」这套照抄过去，
三位立刻和猴与鱼像一家人了。这一条已写进导出器生成的画风参数里。

**怎么定的**：九张候选（三个角色 × 三个方向：毛绒圆胖 / 保原造型 / 性格外放），
每张都要求「画 → 渲染 → 亲眼看 → 改」至少三轮，判好坏一律以**游戏里的真实尺寸**为准
（蛋只有 48×54px，蛇 69×57px——画一堆 1px 花纹在游戏里全糊成一团）。
最后三位都选了「毛绒圆胖」，顺带满足了「三位之间看得出是一家人」这条。

**canon 的三个标记都在，而且补了一个原来漏掉的**：
- 驴：耳挂「正版」吊牌、面无表情。新版把吊牌挪到**左耳**——驴是从屏幕右边探头进来的，
  右边那一截会被屏幕边缘切掉，旧版的右耳吊牌在游戏里一直是缺一半的。
- 蛋：名牌上 `egg` 被红线划掉、留下「蛋」。
- 蛇：小礼帽 **+ 肚子上的蛋形轮廓**。后者是 canon 里写着而原来的图**根本没画**的
  （吞过蛋、被蛋纠正冗余烦到吐出，拒绝解释）。新版画成一个含蓄的浅色椭圆——
  canon 说「拒绝解释」，所以它就该是看得见但不点破的。
- 选定后我把蛇的帽子又改高改窄了一次：候选原稿是矮扁的便帽，canon 写的是小礼帽。

**工具**（都在会话的 scratchpad 里，下次重做角色可以直接抄这套做法）：
`preview.mjs` 出三联图（放大 / 游戏真实尺寸 / 和猴与鱼并排比画风）并跑硬检查
（平涂、自包含、无 emoji、无渲染报错）；`sheet.mjs` 把九张拼成对照大图；
`apply.py` 贴回正本时断言 id / viewBox / 自包含 / 平涂四项。
**viewBox 与 symbol id 必须原样保留**——游戏里 `<use>` 是按固定宽高摆的，改了会变形。
